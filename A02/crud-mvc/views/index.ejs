<!DOCTYPE html> 
<html lang="es"> 
<head> 
  <meta charset="UTF-8"> 
  <title>Usuarios</title> 
</head> 
<body> 
  <h1>Lista de Usuarios</h1> 
  <a href="/nuevo">Nuevo Usuario</a> 
  <table border="1"> 
    <tr> 
      <th>ID</th><th>Nombre</th><th>Correo</th><th>Acciones</th> 
    </tr> 
    <% users.forEach(user => { %> 
      <tr> 
        <td><%= user.id %></td> 
        <td><%= user.nombre %></td> 
        <td><%= user.correo %></td> 
        <td> 
          <a href="/editar/<%= user.id %>">Editar</a> 
          <a href="/eliminar/<%= user.id %>" onclick="return 
confirm('¿Eliminar?')">Eliminar</a> 
        </td> 
      </tr> 
    <% }) %> 
  </table> 
</body> 
</html>